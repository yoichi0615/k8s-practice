apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp
  namespace: k8s-practice
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - backend:
          service:
            name: webapp
            port:
              name: http
        path: /
        pathType: Prefix